{
  "name": "open-ve-typescript-sdk",
  "version": "1.0.5",
  "description": "Client SDK for Open-VE",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "versionup": "npm version patch && git push --follow-tags",
    "publish": "cp package.json ./dist/ && cd dist && npm publish --access public && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
